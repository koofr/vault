{
  "name": "vault-desktop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd ../vault-web && VAULT_APP=desktop node_modules/.bin/vite --clearScreen=false --port=1420 --strictPort",
    "build": "cd ../vault-web && node_modules/.bin/tsc && VAULT_APP=desktop node_modules/.bin/vite build",
    "preview": "cd ../vault-web && node_modules/.bin/vite preview",
    "tauri": "node_modules/.bin/tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.9"
  }
}
