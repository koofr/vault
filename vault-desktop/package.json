{
  "name": "vault-desktop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd ../vault-web && npm run dev-desktop",
    "build": "cd ../vault-web && npm run build-desktop",
    "preview": "cd ../vault-web && npm run vite preview",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.9"
  }
}
